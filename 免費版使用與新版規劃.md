# 一年級全年國語複習：免費半自動方案

## 已確定的範圍

- 先只做康軒一年級，114 學年度上下學期合併複習，不讓孩子選學期或課次。
- 原始題庫保留年級、學期、課次來源，將來做二年級才啟用分課；目前不擴充二年級內容。
- 已取得網站 18 課、262 個不重複單字詞條、111 個課內詞語項目，並編寫原始 73 句原創句子；目前另建立較少句子的全年覆蓋題庫作為第一輪主題庫。
- 每句漢字均核對為該課或以前課次字詞中的字；全年混合使用時自然也都在一年級範圍內。「一」取自網站詞語「一個」，不另外冒充單字詞條。

## 採用方式

這不是把 HTML 檔安裝進 ChatGPT。網頁與 ChatGPT 分工：

網頁選題、念題 → 孩子在紙上寫 → 家長把照片傳到 ChatGPT → ChatGPT 提供初步核對 → 家長確認 → 回網頁標記需要練的字 → 日後用含有這些字的句子複習。

ChatGPT 支援圖片輸入，但這個方案能否使用免費帳號完成，仍以該帳號當下的圖片功能與使用額度為準。額度不足時仍可人工對答案，不影響網頁的念題和錯字本。這個本機版本不呼叫模型 API，不需要 API 金鑰，也沒有自動把照片傳出去的功能。

## 一輪練習

1. 家長開啟網頁，全年題庫混合選題，先試短句；孩子不看答案。
2. 按播放，孩子聽完在紙上寫；可重播、放慢，不用追求一次聽完。
3. 每題分行並寫題號；建議一格一字，先測 2–3 句，能輕鬆完成再增加。
4. 寫完後拍一張正面、清楚的照片。家長再打開核對區，複製對應題號與答案指令，和照片一起傳給 ChatGPT。
5. 看 ChatGPT 列出的「需要再練」與「需確認」。模糊、反光、遮住或辨不清的字不能直接算錯；必要時重拍局部。
6. 家長核對後，在網頁答案中點選需要練的字。此版採人工點選，不會自動讀取 ChatGPT 回覆或同步兩邊資料。
7. 第一輪檢測後，複製「錯字造句指令」到 ChatGPT，帶入需練字及允許字表，請它另外產生新句。網頁目前的「從現有題庫練習」只是備用，不代表已產生新句；新句這一版先由家長核對、念給孩子，之後再接匯入功能。
8. 覺得已穩定時手動移出加強清單。一次寫對只代表這次成功，不等於已經熟練；沒測過也不能算不會。

## 判讀原則

- 照片能協助檢查字是否相符、是否空白、可能錯字，但不保證辨出所有細小筆畫、部件差異。
- 不因答案提供了某字，就把孩子的模糊筆跡猜成該字。不要自動改正孩子寫出的內容。
- 模型只給初步結果，保留「需確認」，由家長最後核對。
- 靜態照片看不到筆畫的書寫順序，因此不判斷筆順是否正確。
- 不把標點或字寫得不漂亮直接判成不識字。是否需要練字形，和能否聽寫出正確字，分開觀察。

OpenAI 官方也說明視覺模型可能在非拉丁文字、小字、旋轉與其他情況出錯。本工具尚未用孩子真實筆跡量測辨識準確率，不能宣稱已驗證自動批改的可靠度。

## 紀錄怎麼保存

第一版由網頁的瀏覽器本機儲存保存加強字與看過答案的句子，並可匯出、匯入 JSON 備份。清除網站資料、換瀏覽器或換裝置不會自動帶走紀錄，需手動匯入。不要把 ChatGPT 對話或記憶功能當成唯一成績資料庫。

先不收集照片歷史，不自動建立兒童個人帳號。由家長操作 ChatGPT 核對，孩子繼續在紙上寫。

## 目前與後續

目前交付：全年本機 HTML、瀏覽器語音、答案揭示、人工標記錯字、加強練習、紀錄備份，以及把題目帶到 ChatGPT 核對的指令。此 HTML 尚未發布成公開網址；電腦可用瀏覽器開啟，手機正式使用宜再放到可連線的網站。

後續若免費半自動流程用得順，再做：

- 貼回 ChatGPT 核對結果後，先預覽再寫入錯字紀錄，減少手動點選。
- 每字保留多次練習日期、自己寫對／提示後寫對／未寫／疑似錯字／無法判讀；沒有證據時維持未測。
- 每輪混合未測字、需練字及少量已熟字，讓262字都有機會測到，不單純一直亂數抽題。
- 依歷次結果安排稍後重測，達到跨日多次獨立寫對才降低出題頻率。間隔作為可調整規則，非固定教育標準。
- 如未來改成同一網頁拍照並自動判讀，另做後端串接模型 API 與費用上限；不能假設免費 ChatGPT 額度可供自製網頁任意呼叫。

## 官方資料

- [ChatGPT 圖片輸入](https://learn.chatgpt.com/docs/image-inputs)
- [模型圖片判讀與限制](https://developers.openai.com/api/docs/guides/images-vision)
- [API 計費](https://developers.openai.com/api/docs/pricing)

## 最新出題策略：少量句子，盡量完整覆蓋

第一輪不再按每課各出題。以全部262個目標字為集合，挑自然、容易聽寫的句子，在維持可讀長度的前提下，減少句數與重複字，盡量讓所有目標字都出現。這是實用的覆蓋最佳化，不宣稱已證明數學上最少句數。

一個很長的句子雖能塞很多字，卻會增加記憶負擔，因此每句以短句為主，設30個漢字上限；較長句可分逗號段重播。檢測可分多天完成，不需要一次寫完整份題庫。

應分開記錄「句庫涵蓋過」「孩子實際寫過」「這次寫對」「跨日多次寫對」。句庫含有某字，不能直接當作孩子已經會寫。

第二輪只把已確認的需練字當成產題目標，另外組成新句。免費版由家長手動把錯字清單與限制交給ChatGPT，不以付費API自動產題。未來接新句匯入時，程式須重新檢查字表、句長、需練字覆蓋、是否重複舊句；模型自稱合格不能代替程式檢查。
